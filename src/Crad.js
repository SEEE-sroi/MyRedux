import React from 'react';
import Data from './Data';  
function Crad({userss}) {
    // const userssss= userss.products.map(user=> <Data key={user.id} user= {user.description} />)

//   const userssss = userss.products.map((user=>(<li key={userss.id}><div></div> </li>)))
  return (
    <div>
      
    </div>
  )
}

export default Crad;
